import { describe, it } from 'vitest';

// TextureSheet requires THREE.Texture which needs WebGL
describe('TextureSheet', () => {
  it.skip('manages sprite frames', () => {
    // Would test frame registration and UV calculation
  });
});
